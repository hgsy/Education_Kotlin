class Reference {
    String str = new String("hello");
    //new 라는 키워드는 객체를 메모리에 올려준다.
    //이렇게 메모리에 올라간 객체를 인스턴스라고 말함.
    //메모리에 올라간 인스턴스를 가리키고, 참조하고, 레퍼런스하는 변수 = str
    //참조한다는 것은 변수가 그것을 가지고 있는 것이 아니라 가리키고 있는 것
    //클래스들은 모두 참조형
}
